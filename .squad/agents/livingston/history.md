# Livingston — History

## Core Context

- **Project:** A root-level Go meeting transcription CLI/TUI application with Windows WASAPI sidecar validation in progress.
- **Role:** Platform Developer
- **Joined:** 2026-05-27T15:26:08.961Z

## Learnings

<!-- Append learnings below -->
- 2026-05-27: Go TUI/CLI rebuild is low risk for Bubble Tea/Cobra/YAML config/OpenAI transcription; keep these behind internal interfaces for testability.
- 2026-05-27: Built-in system-output capture is the platform risk: Windows WASAPI and Linux Pulse/PipeWire monitor sources are viable, while macOS needs virtual loopback support first or a native ScreenCaptureKit/CoreAudio helper before claiming full capture parity.
- 2026-05-27: Implemented `transcribe/` as a standalone pure-Go module with Cobra/Bubble Tea/YAML, ffmpeg-backed chunk capture, OpenAI multipart transcription, ordered transcript persistence, and hardware-free tests.
- 2026-05-27: A nested `.gitignore` cannot safely ignore a build artifact named the same as its parent directory (`transcribe/transcribe`) without hiding the source tree; remove local binaries after validation or ignore from a parent scope later.
- 2026-05-27: Initial implementation was revised by Danny after Basher's rejection and is now approved; the ffmpeg-backed architecture remains, with short cancellable chunks and a green validation matrix.
- 2026-05-27: Windows DirectShow should never rely on synthetic `audio=default`; parse ffmpeg's dshow list, prefer concrete alternative-name IDs, and surface all dshow audio capture candidates because loopback-vs-mic classification is heuristic.
- 2026-05-27: Basher approved the Windows DirectShow device enumeration/capture fix and Coordinator validation passed; concrete enumerated dshow devices are now the accepted Windows capture path.
- 2026-05-28: Windows build wrappers should pass repo paths into Ubuntu WSL with `WSLENV=TRANSCRIBE_DIR/p` instead of manual `wslpath` argument juggling; the translated `TRANSCRIBE_DIR` keeps spaces safe and avoids batch quoting bugs.
- 2026-05-28: Ubuntu WSL2 on this machine needed `golang-go` installed before the Windows amd64 build would run; the validated toolchain is `go version go1.22.2 linux/amd64`.
- 2026-05-28: Windows amd64 build wrapper validated from PowerShell; `WSLENV=TRANSCRIBE_DIR/p` safely translates the repo path into Ubuntu and the pure-Go build lands at `transcribe/build/windows-amd64/transcribe.exe`.
- 2026-05-28: The device-picker Enter flow now applies the selected mic/output device and returns to Settings immediately, and `--logging 1` writes `transcribe.log` in the current working directory for session/chunk/OpenAI diagnostics.
- 2026-07-03 (Danny §4b — Python-free standalone build): Shipped the Windows double-click console guard + Python-helper removal on my slice.
  - **Files I own:** `transcribe/cmd/transcribe/main.go` (calls `attachParentConsole()` first), NEW `transcribe/cmd/transcribe/console_windows.go` (`//go:build windows`) + `transcribe/cmd/transcribe/console_other.go` (`//go:build !windows` no-op), `transcribe/internal/cli/root.go` (dropped `TRANSCRIBE_WINDOWS_PYTHON`/`TRANSCRIBE_WINDOWS_AUDIO_HELPER` reads + `PythonPath`/`HelperScriptPath` fields + now-unused `"strings"` import; kept `runtime.GOOS` + `speaker`), `build_transcribe_win64.bat` (deleted audio_capture.py copy + PYTHON_BOOTSTRAP + %BUILD_VENV% + pip install pyaudiowpatch numpy + all their error blocks; unchanged WSL `CGO_ENABLED=0` build step), `transcribe/README.md`.
  - **Console guard approach:** check `GetConsoleWindow()` via stdlib `syscall.NewLazyDLL("kernel32.dll")`; if 0, `AttachConsole(ATTACH_PARENT_PROCESS)` where the constant is `^uintptr(0)`; reopen `os.Stdout`/`os.Stderr`→`CONOUT$` and `os.Stdin`→`CONIN$` via `syscall.CreateFile` + `syscall.StringToUTF16Ptr`. On `AttachConsole` failure (detached parent / Task Scheduler) write a breadcrumb to `transcribe-console.log` and return — never crash.
  - **Dependency choice:** stdlib `syscall`, NOT `golang.org/x/sys/windows`, because `x/sys` is only an *indirect* dep in go.mod (Danny's rule: don't promote indirect→direct for this). No new import.
  - **Launcher shipped:** `build_transcribe_win64.bat` now emits `%BUILD_DIR%\transcribe.cmd` with body `@echo off\r\ncmd /k "%~dp0transcribe.exe" %*` (generated with doubled `%%` in the .bat so the written file has single `%`). `/k` keeps the console so the transcript-on-quit stays visible. Binary stays plain CONSOLE-subsystem (no `-H windowsgui`).
  - **Coordination gotcha for Rusty/Basher:** Danny's plan has Rusty removing the `PythonPath`/`HelperScriptPath` fields from the `ExternalRecorder` struct while I remove the *assignments* — different files, safe. My construction literal is already minimal (`audio.ExternalRecorder{Platform: runtime.GOOS}`), so `go vet ./cmd/... ./internal/cli/...` and `GOOS=windows go vet ./cmd/transcribe/...` both pass green regardless of Rusty's struct state. If Rusty also drops `Platform`, then `runtime` import in `root.go` must go too (flag for follow-up).
  - **Validation (WSL2 Ubuntu go1.22.2):** `go vet ./cmd/... ./internal/cli/...` rc=0; `go test ./internal/cli/...` ok (cached) rc=0; `GOOS=windows go vet ./cmd/transcribe/...` rc=0 (confirms the windows-tagged guard parses). Full `go build ./...` is Rusty's slice + Basher's gate.
- 2026-07-03 (follow-up — Cobra mousetrap still blocked double-click): The console guard from the previous slice was insufficient; Karlis still saw "This is a command line tool / You need to open cmd.exe and run it from there."
  - **Root cause:** `github.com/spf13/cobra@v1.9.1/command_win.go::preExecHook` fires when `MousetrapHelpText != "" && mousetrap.StartedByExplorer()`, which checks whether the PARENT process is `explorer.exe` (a double-click), NOT the console state. It prints `cobra.MousetrapHelpText` (defined in `cobra.go`) and `os.Exit(1)` before the TUI runs. So `attachParentConsole()` alone could never fix a double-click — the parent is explorer.exe regardless of console attachment.
  - **Fix:** set `cobra.MousetrapHelpText = ""` early in `main()` (`cmd/transcribe/main.go`), AFTER `attachParentConsole()` and BEFORE `cli.NewRootCommand(...)`. Cobra's own doc comment above the var confirms "To disable the mousetrap, just set this variable to blank string". No `//go:build windows` tag needed — the var assignment lives in build-tag-free `cobra.go`; only the *firing* check is Windows-only (`command_win.go`). Harmless no-op on Linux/macOS.
  - **Why main.go vs internal/cli/root.go:** the only entry point is `main.go`, and the concern pairs 1:1 with `attachParentConsole()` (both are process-launch ergonomics for the double-click path). Keeping both adjacent in `main()` makes the intent self-documenting; no need to thread it through every command. Minimal change per scope.
  - **Two changes work together:** mousetrap-blanked (no early exit) + attach-console (real TTY for Bubble Tea). Don't drop either.
  - **Validation gap:** double-click cannot be reproduced from a terminal (parent isn't explorer.exe), so proof is (a) source review of cobra `command_win.go` + the blanked var, and (b) `go build`/`go vet`/`go test` green + `GOOS=linux GOARCH=amd64` cross-compile still green. Real double-click confirmation is Karlis's after ship.
- 2026-07-03 (config-source startup line + output-capture fallback regression fix): Two asks from Karlis shipped together.
  - **Task 1 — config-source line:** `config.Load` now returns `(Config, []string, Source, error)` — added a new `config.Source{Path, Explicit, Found}` return value (`transcribe/internal/config/config.go`). `Found` is its OWN field, NOT a warning, so `TestInvalidEnvironmentFallsBackWithWarning`'s `len(warnings)==2` assertion stays intact. `Source.SummaryLine()` renders the one-line banner (`Config loaded from <path>` / `Config <path> not found; using defaults and environment variables` / `Config not found; using defaults and environment variables`). Emitted in `transcribe/internal/cli/root.go::run` AFTER `config.Load` and AFTER the `--list-devices` early return, to stdout (`ioStreams.Stdout`) + `logging.Printf`, before `app.Prepare`. **Deliberate skip list:** `--version` (handled before `run()` is ever called, so the line is inherently suppressed); `--list-devices` (suppresses it to keep device output clean — stated choice for Karlis). `--silent` and TUI runs both print it on stdout so the line prefixes the session. Also folded in a cleanup: root.go now seeds `opts.configPath` from `source.Path` (instead of re-resolving via `config.DefaultPath()`), so the returned Source is the single source of truth for the resolved path.
  - **Task 2 — output-capture fallback regression (the "audio devices again" cascading fallback churn):** Root bug was that `devicesFromWindowsAudioEndpoints` sets `device.Name = windowsLoopbackDisplayName(raw)` i.e. `"<render> [Loopback]"`. The `looksLikeWindowsLoopback(device.Name)` classifier in `audio.go` then matches the literal "loopback" token in that *poisoned display name* and was treating display-only synthesized Windows render endpoints (Speakers/Headphones [Loopback]) as if they were real ffmpeg-openable sources — so the runtime fallback chain cycled through `audio=Speakers (...)` → `ffmpeg: Could not find audio only device` → "falling back" → next synthesized endpoint → same failure → … churn.
  - **Fix scope (RUNTIME ONLY — discovery/display deliberately untouched):** Added three helpers in `transcribe/internal/audio/audio.go`: `SynthesizedDirectShowRenderEndpoint(device)` (true iff Source==Output && Backend=="dshow" && no loopback signature in ID/aliases — INSPECTS ID/ALIASES, NOT the display name, because the display name is poisoned with "loopback"); `runtimeCapableOutputDevice(device)` (true for non-dshow, true for dshow only when NOT synthesized); `OutputCaptureUnavailableGuidance(devices)` (returns `windowsOutputCaptureWarning` when outputs exist but none are runtime-capable; empty otherwise — testable on Linux because it inspects device metadata, not GOOS). Modified `OutputCaptureCandidates` to filter its candidate pool through `runtimeCapableOutputDevice` before ordering — current/self may still surface at index 0 (so the failure message still names the attempted device and `nextOutputCaptureCandidate` can index [1] to the next real source), but every actual fallback TARGET candidates[1:] is real. Modified the output-capture error path in `transcribe/internal/app/session.go` to append the actionable guidance (` — <windowsOutputCaptureWarning>`) when `OutputCaptureUnavailableGuidance` fires, so users see Stereo Mix / VB-CABLE / Voicemeeter / `--list-devices` instead of the bare `Could not find audio only device` line.
  - **What I did NOT touch (display/selection invariant):** `looksLikeWindowsLoopback`, `windowsOutputSelectionWarning`, `windowsLoopbackDisplayName`, `devicesFromWindowsAudioEndpoints`, `supplementWindowsOutputCapture`. Discover/`--list-devices`/the device picker still surface `Speakers (...) [Loopback]` so users can see render endpoints; we just never feed those into the runtime ffmpeg capture path or the runtime fallback chain. `TestWindowsAudioEndpointRenderCandidatesBecomeLoopbackOutputs` (warn==0 + `[Loopback]` display name) still passes green.
  - **Why I added an exported `SynthesizedDirectShowRenderEndpoint` (vs a private helper):** the test suite asserts on it directly to pin the "inspect ID+aliases, NOT the poisoned display name" invariant, and Basher's existing `windowsOutputCaptureWarning`-literal test pattern in `discover_test.go` mirrored well. Exporting it also lets `SelectDevices`/future logic reuse the classifier without re-deriving it.
  - **Files touched:** `transcribe/internal/config/config.go` (Source type + Load signature + SummaryLine), `transcribe/internal/config/config_test.go` (3 existing callers updated to 4-tuple + 1 new `TestLoadExplicitMissingReportsNotFound` + Source assertions), `transcribe/internal/cli/root.go` (4-tuple return consumed + source.Path seeding + `Fprintln`/`logging.Printf` banner), `transcribe/internal/audio/audio.go` (3 new helpers + OutputCaptureCandidates filter), `transcribe/internal/audio/discover_test.go` (5 new tests — ExcludesSynthesizedRenderEndpoints, KeepsSelfWhenOnlySynthesizedDevicesRemain, KeepsNonDShowBackends, ChecksIDNotPoisonedDisplayName, GuidanceFires/Silent), `transcribe/internal/app/session.go` (error path appends OutputCaptureUnavailableGuidance), `transcribe/internal/app/app_test.go` (1 new `TestSynthesizedRenderEndpointsSurfaceActionableError` asserting cascading-fallback is collapsed + the error carries Stereo Mix/VB-CABLE/--list-devices + output recorder exercised ≤3 times).
  - **Validation:** Windows PowerShell Go 1.26.4 — `go build ./...` clean; `go vet ./...` clean; `go test ./...` all 11 packages ok including the 7 new tests. WSL cross-compile smoke skipped (go is present natively on Windows, so the task's "only if go missing" branch is N/A).
  - **Reusable rule surfaced:** the synthesized-vs-runtime-capable distinction is now codified as the team decision below + the extracted `audio-classification` skill.
- 2026-07-06: Synthesized-only Windows output capture unavailability is a non-fatal runtime condition, not a session error: emit one output-disabled warning with the canonical Stereo Mix / VB-CABLE / Voicemeeter / `--list-devices` guidance, call `disableOutputCapture()`, and keep microphone transcription running; still fail fast for generic output backend errors when a runtime-capable output device exists.
- 2026-07-06 (vNext WASAPI sidecar first slice): Started the Windows recorder sidecar architecture without reintroducing Python or cgo in the main app. Added backend constants including `wasapi-loopback`; Windows render endpoints discovered from PowerShell are now display/selection candidates for the `wasapi-loopback` sidecar, while real DirectShow loopbacks (`Stereo Mix`, VB-CABLE, Voicemeeter, `virtual-audio-capturer`) remain the implemented fallback path. `ExternalRecorder` now dispatches only `windows + SourceOutput + BackendWasapiLoopback` chunks to a `wasapi-loopback-recorder.exe record --output-file ... --duration ... --sample-rate 16000 --channels 1 --device-id ...` protocol; all mic/Linux/macOS/DirectShow paths still use ffmpeg. The helper executable exists as a Windows-only protocol-valid stub and intentionally exits with “native Windows WASAPI loopback capture is not implemented yet” so we do not ship fake audio. Runtime guidance disables output and keeps mic transcription alive when only sidecar candidates exist and native capture is missing. Build script now builds/stages the helper beside `transcribe.exe`; launcher now references `TRANSCRIBE_WINDOWS_WASAPI_HELPER` instead of the removed Python env vars.
- 2026-07-06 (native Go WASAPI sidecar implementation): Replaced the `wasapi-loopback-recorder.exe` Windows stub with a pure-Go/no-cgo COM WASAPI loopback recorder in `transcribe/cmd/wasapi-loopback-recorder`. It initializes COM, enumerates/selects active render endpoints by default/MMDevice ID/friendly/display name, activates `IAudioClient` in shared mode with `AUDCLNT_STREAMFLAGS_LOOPBACK`, reads packets through `IAudioCaptureClient`, converts native PCM/float mix formats to PCM16 16 kHz mono WAV chunks, and adds `list --json`. Main app remains pure-Go/cgo-free and still shells out to the helper only for `BackendWasapiLoopback` output chunks; ffmpeg remains for mic/DirectShow/Linux/macOS. `BackendWasapiLoopback` is now runtime-capable, helper validation checks packaging, and docs now frame Stereo Mix/VB-CABLE/Voicemeeter as fallback diagnostics rather than the primary stock-Windows path. Hardware-free tests cover helper args, WAV finalization, conversion, extensible mix-format parsing, and audio dispatch; live Realtek smoke testing is still the manual gate.
- 2026-07-06: Platform/build navigation is root-level Go now: `go.mod` is at repo root; `cmd/` contains the app and WASAPI sidecar entrypoints; `internal/` contains CLI/TUI/platform packages; `docs/` contains test plans; `build_transcribe_win64.bat` stages generated Windows artifacts under `build/windows-amd64/`. Treat `transcribe/` as obsolete except stale artifacts under `transcribe/build/` if present.
- 2026-07-06: README now reflects the root Go module layout after removing the nested `transcribe/` tree: build/test commands run from repository root, Windows packaging lands in root `build/windows-amd64/`, and the layout section names root `cmd/*`, `internal/*`, and `docs` paths.
